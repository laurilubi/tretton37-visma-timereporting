// Returns the full url, based on the active customization
// relativeUrl is relative to /customization/*, eg /customization/tretton37
function url(relativeUrl) {
    return "{{customizationBaseUrl}}" + relativeUrl;
}
